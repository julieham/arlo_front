import {UpperBoundDirective} from './upper-bound.directive';

describe('UpperBoundDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperBoundDirective();
    expect(directive).toBeTruthy();
  });
});
