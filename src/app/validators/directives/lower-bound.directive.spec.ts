import {LowerBoundDirective} from './lower-bound.directive';

describe('LowerBoundDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerBoundDirective();
    expect(directive).toBeTruthy();
  });
});
